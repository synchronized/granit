<!-- SPDX-License-Identifier: MIT -->
<!-- Copyright (c) 2026 Granit contributors -->

# 项目文档规范

## 目的

本规范用于约束项目文档的职责、组织、修改范围和生命周期。目标是让读者快速找到唯一可信的
信息来源，并避免 README、架构、路线图、计划和实现记录相互复制。

本文件主体不依赖 Granit 的技术栈，可以复制到其他项目使用；项目特有的目录映射位于文末。

## 核心原则

### 单一职责

每份文档只回答一种主要问题。若一份文档同时承担入门、接口参考、架构决策、实施计划和历史记录，
应按职责拆分。

### 单一事实来源

同一事实只能有一个权威来源。其他位置可以提供一句摘要和链接，但不得复制会独立演化的完整内容。
尤其不要在 README、路线图和计划中分别维护同一份任务进度。

### 高内聚、低耦合

- 同一主题的说明、约束和示例尽量位于同一文档。
- 文档通过稳定入口和相对链接关联，不依赖其他文档中的临时段落。
- 下层功能文档不依赖路线图状态；计划变化不应迫使 API 参考文档同步修改。
- 每次修改少量文件是良好职责划分的结果，不作为禁止必要同步的硬性指标。

### 区分现状、未来与历史

- 功能和参考文档只描述仓库当前真实行为。
- 计划文档描述尚未完成或正在实施的方案。
- 路线图描述优先级、依赖和状态摘要。
- 实施记录描述已经发生的过程、验证结果和差异。
- 不把计划能力写成已经实现，也不让历史日志淹没当前使用说明。

## 文档类型

采用面向读者目的的分类方式：

| 类型 | 主要问题 | 应包含 | 不应包含 |
|---|---|---|---|
| 项目 README | 这是什么，如何开始 | 定位、状态、特点、最小示例、文档入口 | 完整索引、长期进度日志 |
| 文档中心 | 文档在哪里，从何读起 | 分类索引、推荐阅读路径 | 功能实现细节 |
| 教程 Tutorial | 如何从零完成一个完整目标 | 连续步骤、预期结果 | 完整 API 枚举 |
| 指南 Guide | 如何解决一个具体问题 | 前置条件、步骤、排错 | 架构历史 |
| 参考 Reference | 接口的准确行为是什么 | 参数、返回值、所有权、限制 | 未确认方案 |
| 概念 Concept | 为什么这样设计，模块如何协作 | 模型、边界、关系、权衡 | 逐次实施日志 |
| 路线图 Roadmap | 先做什么，现在到哪里 | 阶段、优先级、状态摘要 | 每个任务的详细步骤 |
| 计划 Plan | 某项能力准备如何实现 | 目标、非目标、步骤、验收 | 无限追加的完成日志 |
| 决策 ADR | 为什么选择某一方案 | 背景、决策、影响、替代方案 | 使用教程 |
| 记录 Record | 实际执行和验证结果是什么 | 日期、环境、结果、差异 | 当前 API 的权威定义 |

教程、指南、参考和概念四类可以采用 Diátaxis 思路组织。项目不必为了分类立即移动所有文件，但新建
或拆分文档时应明确其类型。

## 推荐目录

```text
README.md                    # 项目入口
DOCUMENTATION_GUIDE.md       # 文档规范
docs/
├─ README.md                 # 文档中心与推荐阅读顺序
├─ tutorials/                # 完整学习教程
├─ guides/                   # 面向任务的操作指南
├─ reference/                # API、格式与行为参考
├─ concepts/                 # 架构与原理说明
├─ plans/                    # 未完成或实施中的计划
├─ decisions/                # 架构决策记录 ADR
└─ records/                  # 实施、测量与迁移记录
```

现有项目可以渐进迁移，不应只为匹配目录而进行一次无价值的大规模重命名。

## 链接与依赖方向

文档入口采用由总到分的单向结构：

```text
README.md
  -> docs/README.md
       ├─ Tutorial / Guide
       ├─ Reference / Concept
       ├─ Roadmap -> Plan
       └─ ADR / Record
```

- 根 README 原则上只链接文档中心、快速开始所需页面和少量关键入口。
- `docs/README.md` 负责完整分类索引，根 README 不重复全部链接。
- Roadmap 链接详细 Plan；Plan 不复制 Roadmap 的全局任务清单。
- Concept 可以链接 ADR 解释决策来源；ADR 保持不可变历史，不承担当前完整架构说明。
- Record 可以链接提交、测试或基准结果，但其他文档不应依赖 Record 才能理解当前行为。
- 使用相对链接；移动文件时必须同步检查入链与出链。

## 修改范围

先找到事实的权威文档，再决定修改范围：

| 变化 | 通常修改 |
|---|---|
| 公共 API 或行为变化 | 对应 Reference；必要时同步 Guide 或示例 |
| 架构边界变化 | ADR；决策稳定后更新 Concept 摘要 |
| 实施方案变化 | 对应 Plan |
| 任务状态变化 | Plan 状态与 Roadmap 摘要 |
| 构建或操作流程变化 | 对应 Guide |
| 项目定位或最小入口变化 | 根 README |
| 测试、性能或迁移结果 | Record 或专用结果目录 |

不要因为代码改动就机械更新所有上层文档；也不要为了追求“只改一个文件”而遗漏确实发生变化的
权威说明。

## 内容与排版

- 先写结论、用途或当前状态，再补充细节。
- 长段落优先拆成有语义的短段落、项目列表、表格或小型关系图。
- 列表用于并列事实，编号用于有顺序的步骤；不要把连续论证强行拆成碎片列表。
- 树状结构用于目录、模块或所有权关系，流程图用于存在三个以上连续阶段的执行关系。
- 标题应表达内容，不使用连续多个“其他”“补充”“说明”。
- 链接文字应描述目标内容，不使用大量“点击这里”。
- 文档示例必须与当前接口一致；伪代码应明确标记为草案或概念示意。
- 项目术语保持一致；同一概念不要交替使用多个未定义名称。
- 遵守项目规定的语言、换行、许可证头和单行长度。

## 篇幅约束

篇幅是拆分提示，不是机械限制：

- 根 README 建议不超过 180 行。
- Tutorial、Guide、Reference 和 Plan 建议不超过 250 行。
- ADR 建议不超过 100 行。
- 超过 300 行时必须检查是否混合了不同文档类型。
- 连续新增多个“实现记录”章节时，应拆到 Record，而不是继续扩张 Plan。

拆分后每个文件必须仍能独立说明自己的主题，不能把一个连续段落任意切成多个互相强依赖的文件。

## 文档生命周期

### 新建

1. 确认读者、主要问题和文档类型。
2. 搜索是否已有权威文档，优先扩展而不是创建重复入口。
3. 选择稳定名称和目录，补充到 `docs/README.md` 的对应分类。
4. 明确当前能力与未来计划的边界。

### 实施中

- Plan 只维护当前设计、步骤、未决问题和验收状态。
- 重要且可复用的决策写入 ADR。
- 大段逐次提交记录、命令输出和性能数据写入 Record 或结果目录。

### 完成

1. 将已经验证的长期事实更新到 Reference、Guide 或 Concept。
2. 在 Plan 中保留简短完成状态和与原计划的主要差异。
3. 将详细实施过程迁移到 Record；不要删除仍有价值的历史依据。
4. Roadmap 只更新状态摘要和下一优先级。

### 失效

- 当前文档不得继续链接已失效方案作为现行规范。
- ADR 通常不删除，改为“已取代”并链接新决策。
- 完全失效的 Plan 可以移入归档目录，并注明取代者。

## 文档模板

### Plan

```markdown
# 任务编号：标题

## 状态

## 背景与目标

## 非目标

## 已确认决策

## 实施顺序

## 测试与验收

## 风险与未决问题
```

### ADR

```markdown
# ADR-000：决策标题

- 状态：提议 / 已接受 / 已取代
- 日期：YYYY-MM-DD

## 背景

## 决策

## 影响

## 替代方案
```

### Guide

```markdown
# 任务标题

## 适用场景

## 前置条件

## 操作步骤

## 验证结果

## 常见问题
```

## 检查清单

提交文档前检查：

- 文档是否只有一个主要职责？
- 当前行为、未来计划和历史记录是否明确分离？
- 是否复制了其他文档中会持续变化的内容？
- 是否链接到正确的权威来源？
- 新文档是否加入文档中心，而不是直接堆入根 README？
- 标题、列表、表格和代码块是否比大段文字更易读？
- 文档描述是否与仓库当前实现一致？
- 相对链接、锚点、代码示例和命令是否有效？
- 修改范围是否覆盖所有真正受影响的权威文档？

## 自动化建议

项目可逐步在 CI 中加入：

- Markdown 格式和尾随空白检查。
- 相对链接、标题锚点和孤立文档检查。
- 根 README 篇幅及直接文档链接数量提示。
- Plan、ADR 等类型的必要元数据检查。
- 公共 API 变化与 Reference 更新提示。

自动化应优先报告明确错误。篇幅、链接数量和文档更新关联通常作为提醒，避免为满足指标进行无意义
拆分或空洞修改。

## Granit 项目映射

Granit 当前采用渐进整理方式：

- 根 `README.md` 后续精简为项目入口，并统一指向 `docs/README.md`。
- `docs/concepts/architecture.md` 是架构 Concept 的当前权威来源。
- `docs/roadmap.md` 只保留阶段、优先级、状态摘要和近期顺序。
- `docs/guides/` 保存操作指南，`docs/reference/` 保存当前 API 与行为参考，
  `docs/concepts/` 保存架构和原理说明。
- `docs/plans/*.md` 保存设计和实施计划；大段实现日志后续迁移到 `docs/records/`。
- `benchmarks/results/` 继续保存可复现性能结果，不复制到计划正文。

Granit 已通过 `cmake/check_documentation.cmake` 和 `granit.documentation` CTest 检查自有
Markdown 相对文件链接、根 README 篇幅及分类索引完整性。未来措辞和文档类型判断暂不作为硬错误，
避免误伤合理的限制与兼容性说明。

当前入口、Guide、Reference、Concept、Plan 和 Record 已按职责分层；后续继续拆分 H-07、H-02
和 H-03 中混合的实现记录。
